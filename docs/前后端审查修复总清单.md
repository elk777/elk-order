# 前后端代码审查修复总清单

> 日期：2026-06-26
> 前端分支：`codex/frontend-review-fixes`
> 后端分支：`codex/backend-review-fixes`

## 前端修复

来源报告：`docs/前端代码审查报告.md`

已处理范围：
- 高严重缺陷 H1-H8：订单归一化、token 刷新队列、日期筛选、下单清购物车误报、CateList 数据流、Upload 删除/回传、情侣空值兼容。
- 中低严重缺陷：订单分页竞态、loadMore 回退、菜谱封面上传竞态、封面未上传完成提交、响应 `data:null` 保留、购物车 ID 比较、延迟跳转清理、菜谱详情稳定 key、积分未知类型兜底。
- 安全/性能/维护：请求日志脱敏、Pinia 持久化收敛、首页 onShow 并行、搜索防抖、微信 `urlCheck:true`、App 权限收敛、死文件删除、调试日志清理、全局错误日志兜底。

详细清单：`docs/前端审查修复清单.md`

## 后端修复

来源报告：`/Users/lucky_elk/Desktop/elk_project/H-K-T/hkt-applet-service-nest/docs/后端代码审查报告.md`

已处理范围：
- 发布阻断安全项：鉴权后门隔离、生产禁用 mock-login、用户列表默认拒绝普通用户、关闭客户端任意积分收入、移除用户自改绑定/角色。
- 并发一致性：积分原子条件更新、情侣绑定事务内校验和加锁、订单状态条件更新防覆盖。
- 稳定性：异常脱敏并记录 5xx 日志、启动配置 fail-fast、生产 CORS 禁止 `*`、shutdown hooks、进程异常日志、健康检查。
- 性能/维护：订单/积分/情侣查询索引、订单关联用户字段白名单、菜谱缓存写后失效、BigInt 参数 400、通知已读 404、又拍云默认 HTTPS、删除一次性脚本。

详细清单：`/Users/lucky_elk/Desktop/elk_project/H-K-T/hkt-applet-service-nest/docs/后端审查修复清单.md`

## 验证

- 前端已执行 `git diff --check`，通过。
- 后端已执行 `pnpm typecheck`、`pnpm build`、`pnpm lint`、`git diff --check`，均通过。
- 前端 `package.json` 当前没有 lint/test/typecheck 脚本，无法运行自动化测试。

## 未自动落地

- 前端：ESLint/Prettier/Vitest/CI、完整 `api/`/`apis/` 目录迁移、超大页面拆分、主题色全量替换、上传 hook 抽象、token 加密策略。
- 后端：限流/helmet、Jest/Supertest、Swagger、Prisma enum 化、下单幂等键、repository 层、严格 TS/ESLint。
- 这些项都涉及新增依赖、数据库契约或较大结构迁移，建议单独拆任务 review。

## 分支与合并

- 已按要求在前后端分别维护修复分支。
- 未合并、未 push。
- 等你 review 确认后再进入合并或提交流程。
